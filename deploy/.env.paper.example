# Copy to deploy/.env.paper (gitignored) and fill in. Loaded by `make up-paper`.
# Paper reuses the current paper user — same values you run today.
DATABENTO_API_KEY=db-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
TWS_USERID=your-paper-username
TWS_PASSWORD=your-paper-password
IBKR_ACCOUNT_ID=DUXXXXXXX
