# tests/

Tests for the checks in `bin/`. Each check has at least one test that shows it refusing.

- **Goes in:** `test_<check>.sh` or `test_<check>.py` per check
- **Stays out:** fixtures with real ids or hosts
