# instances/

One directory per running instance. `example/` is the template; real instances are private and never tracked here.

- **Goes in:** `example/` only, in the public tree
- **Stays out:** any instance that has ever touched a real broker session
